str main() {
    num a = 12;
    if( a <= 10 ) {
        return "smööl";
    } else {
        num c1 = 0;
        num c2 = 0;
        num cq = 0;

        for(num n in [1, 2, 3, 4]) {
            if( n == 1 ) {
                c1 = c1 + 1;
            } else if ( n == 2 ) {
                c2 = c2 + 1;
            } else {
                cq = cq + 1;
            }
        }

        if( (c1 == 1) and (c2 == 1) and (cq == 2) ) {
            return "CORRECT";
        } else {
            return "INCORRECT";
        }
    }
    return "LOLEXTRA";
}