######### START

label:
    push-value (4, 5)
    push-value (4, 5)
    store-global msg
    push-value 10
    push-value 0
    loop:
        dup-2
        is-less
        if-false exit-loop
            push-value 1
            add
            dup-1
            push-value -1.0
            rot-2
            add
            call print
            pop-1
            jump loop
        exit-loop:
            jump exit-label
exit-label:
    load-global msg
    call print
    pop-1
    return

######### END