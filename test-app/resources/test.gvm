label:
    push 5
    push 0
    loop:
        dup 2
        is-less
        if-false exit-loop
            push 1
            add
            jump loop
        exit-loop:
            jump exit-label
exit-label:
    push 2
    sub
    neg
    push 3
    is-equal
    return